# Simple Interest Calculator

This repository contains a bash script that calculates simple interest based on user input.

## Usage
Run the script from the terminal:
./simple-interest.sh

You will be prompted to enter the principal amount, rate of interest, and time period. The script will then output the calculated simple interest.
